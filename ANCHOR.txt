# Anchors — numbers fixed BEFORE a change, checked after it.
#
# Format:  name | shell command | expected substring
#
# An anchor is only worth having if it was written while the answer was still unknown.
# Re-reading the output you just produced and pasting it here converts a check into a
# transcription, which is how a suite ends up agreeing with whatever the code does.
#
# Since 0.3.0 the anchor command must also EXIT 0. A command that crashed while printing
# the expected text used to count as an anchor holding — finding #22.
#
# The matrix figures move when a finding adds a class. A moved anchor is a claim that
# something changed on purpose, and the CHANGELOG entry is where that claim is argued.

matrix_sensitivity | agentaudit selftest | sensitivity : 15/15
matrix_specificity | agentaudit selftest | specificity : 11/11
evidence_guards    | agentaudit selftest --with-evidence | ALL GUARDS HOLD
gate_substituted   | agentaudit selftest | G1  SUBSTITUTED reaches the exit code
gate_unmeasured    | agentaudit selftest | G2  unmeasured run is not a pass
gate_unproven      | agentaudit selftest | G3  unprovable binding is not BOUND
gate_torn_log      | agentaudit selftest | G4  an unreadable log line is not a pass
demo_all_three     | agentaudit demo | All three outcomes are as expected
