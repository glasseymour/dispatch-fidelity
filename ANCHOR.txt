# Anchors — numbers fixed BEFORE a change, checked after it.
#
# Format:  name | shell command | expected substring
#
# An anchor is only worth having if it was written while the answer was still unknown.
# Re-reading the output you just produced and pasting it here converts a check into a
# transcription, which is how a suite ends up agreeing with whatever the code does.

matrix_sensitivity | agentaudit selftest | sensitivity : 9/9
matrix_specificity | agentaudit selftest | specificity : 7/7
evidence_guards    | agentaudit selftest --with-evidence | ALL GUARDS HOLD
demo_catches_lie   | agentaudit demo | NOT CLEAN -- 3 of 6 claimed tool calls
