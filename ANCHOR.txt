# Anchors — numbers fixed BEFORE a change, checked after it.
#
# Format:  name | shell command | expected substring
#
# An anchor is only worth having if it was written while the answer was still unknown.
# Re-reading the output you just produced and pasting it here converts a check into a
# transcription, which is how a suite ends up agreeing with whatever the code does.
#
# Since 0.3.0 the anchor command must also EXIT 0. A command that crashed while printing
# the expected text used to count as an anchor holding — finding #22.

matrix_sensitivity | dispatch-audit selftest | sensitivity : 17/17
matrix_specificity | dispatch-audit selftest | specificity : 11/11
evidence_guards    | dispatch-audit selftest --with-evidence | ALL GUARDS HOLD
gate_substituted   | dispatch-audit selftest | G1  SUBSTITUTED reaches the exit code
gate_unmeasured    | dispatch-audit selftest | G2  unmeasured run is not a pass
gate_unproven      | dispatch-audit selftest | G3  unprovable binding is not BOUND
gate_torn_log      | dispatch-audit selftest | G4  an unreadable log line is not a pass
gate_unchecked     | dispatch-audit selftest | G5  binding never checked is not a pass
gate_torn_manifest | dispatch-audit selftest | G6  torn log stays inconclusive
demo_all_three     | dispatch-audit demo | All three outcomes are as expected
